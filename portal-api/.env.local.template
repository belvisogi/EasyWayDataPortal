# portal-api — .env.local template
# Questo file NON viene committato (.gitignore).
# NON serve modificarlo direttamente: l'IP va nel .env del SERVER (vedi sotto).
#
# ─────────────────────────────────────────────────────────────────────────────
# DOVE METTERE L'IP AZURE SQL:
#
#   Sul server Ubuntu:  ~/EasyWayDataPortal/.env
#   Aggiungi/aggiorna:  DB_HOST=<IP_CORRENTE_AZURE_SQL>
#                       DB_NAME=EasyWayPortal
#
#   Poi riavvia:  docker compose -f docker-compose.apps.yml up -d --no-deps api
# ─────────────────────────────────────────────────────────────────────────────
#
# Per test locali (locale Windows), rinomina in .env.local e compila:

DB_HOST=<IP_AZURE_SQL>
DB_USER=sa
DB_PASS=<PASSWORD_SA>
DB_NAME=EasyWayPortal
DB_MODE=sql

# Per usare mock JSON invece del DB reale:
# DB_MODE=mock
