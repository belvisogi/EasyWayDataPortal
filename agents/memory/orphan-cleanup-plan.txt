# Orphan Tags Cleanup Action

Aggiungerò questa logica a `agent-docs-scanner.ps1`:

```powershell
if ($Action -eq "CleanOrphans") {
    # 1. Audit and save exotic tags
    # 2. Map salvageable ones
    # 3. Propose removal of noise tags (with backup)
}
```

Prima mappo i salvabili velocemente nella hierarchy.
