# Core Agent Tools

Canonical location for cross-agent operational tools:

- `agent-llm-router.ps1`
- `agent-multi-vcs.ps1`
- `agent-branch-coordinator.ps1`

Compatibility wrappers are kept in `scripts/pwsh/` so existing commands and CI jobs continue to work.
