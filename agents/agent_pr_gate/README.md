# agent_pr_gate

**Role**: PR policy gatekeeper

## Overview
Validates pull request constraints and governance checks before merge.

## Usage
Use the manifest action set for gate checks in CI and local preflight.
