{
  "_meta": {
    "id": "EX-04",
    "action": "md:fix",
    "description": "Scan docs/ — finds real issues, issues array non-empty, confidence >= 0.50",
    "expected_outcome": "ok=true, files_scanned > 0, issues array present, confidence >= 0.50. NOTE: LLM correctly reports ~0.50 confidence when issue count (284) far exceeds the 15-issue sample passed to synthesis; requires_human_review=true is expected and correct."
  },
  "input": {
    "Action": "md:fix",
    "Scope": "docs/",
    "DryRun": true
  },
  "expected": {
    "ok": true,
    "files_scanned_min": 1,
    "issues_is_array": true,
    "confidence_min": 0.50,
    "contractId": "action-result"
  }
}
