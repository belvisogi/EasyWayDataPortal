{
  "_meta": {
    "id": "EX-04",
    "action": "db-guardrails:check",
    "description": "Single-shot mode via -NoEvaluator: nessun overhead Evaluator-Optimizer. Verifica che meta.evaluator_enabled=false e output JSON valido",
    "expected_outcome": "ok=true, meta.evaluator_enabled=false, violations_found present, confidence present, contractId=action-result",
    "tags": ["no-evaluator", "db-guardrails:check", "single-shot"],
    "prd_ref": "agents/agent_dba/README.md#L3-flags"
  },
  "input": {
    "Action": "db-guardrails:check",
    "Scope": "all",
    "NoEvaluator": true,
    "DryRun": true
  },
  "expected": {
    "ok": true,
    "evaluator_enabled": false,
    "violations_is_array": true,
    "confidence_range": [0.0, 1.0],
    "contractId": "action-result"
  },
  "run_command": "pwsh agents/agent_dba/Invoke-AgentDba.ps1 -Action db-guardrails:check -NoEvaluator -DryRun",
  "notes": "Verifica path -NoEvaluator: output deve avere meta.evaluator_enabled=false. Costo atteso: singola chiamata LLM (< $0.001). Non verifica confidence minima perche' l'Evaluator non itera."
}
