{
  "_meta": {
    "id": "EX-01",
    "action": "frontend:build-check",
    "description": "Build check frontend stack (deterministic) — expects ok=true/degraded, contractId=action-result",
    "expected_outcome": "ok=true, status in [ok, degraded], contractId=action-result"
  },
  "input": {
    "Action": "frontend:build-check"
  },
  "expected": {
    "ok": true,
    "status_in": ["ok", "degraded"],
    "contractId": "action-result"
  }
}
