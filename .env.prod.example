# 🌍 SOVEREIGN PRODUCTION ENVIRONMENT
# Target: Oracle Cloud (ARM)

# ⚠️  PRODUCTION SECURITY WARNING ⚠️
# DO NOT use plaintext passwords in production!
# Use Azure Key Vault references instead:
# Example: @Microsoft.KeyVault(SecretUri=https://vault.azure.net/secrets/SQL-PASSWORD/)

# --- SECURITY ---
# User for n8n basic auth
N8N_USER=admin
# STRONG password for n8n (use Azure Key Vault in production!)
N8N_BASIC_AUTH_PASSWORD=ChangeMe_Use_Azure_KeyVault_Reference!

# User for PostgreSQL (n8n internal db)
POSTGRES_USER=n8n
POSTGRES_PASSWORD=ChangeMe_Use_Azure_KeyVault_Reference!

# SQL Server SA Password
SQL_SA_PASSWORD=ChangeMe_Use_Azure_KeyVault_Reference!

# MinIO Credentials
MINIO_ROOT_USER=admin
MINIO_ROOT_PASSWORD=ChangeMe_Use_Azure_KeyVault_Reference!

# Qdrant Vector DB API Key
QDRANT_API_KEY=ChangeMe_Use_Azure_KeyVault_Reference!

# Traefik Basic Auth (generate with: htpasswd -nb username password)
# Remember to escape $ with $$ for docker-compose: echo $(htpasswd -nb user pass) | sed 's/\$/\$\$/g'
TRAEFIK_BASIC_AUTH_HASH=ChangeMe_GenerateWithHtpasswd!

# --- INFRASTRUCTURE ---
# The public domain of your instance (NEVER use IP addresses in production!)
DOMAIN_NAME=your-domain.com

# --- API KEYS (Used by n8n and agents) ---
OPENAI_API_KEY=sk-...
DEEPSEEK_API_KEY=sk-...
ANTHROPIC_API_KEY=sk-...
